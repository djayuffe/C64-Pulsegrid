# Lineage

Canonical source: `Pulsegrid.zip`, a Commodore 64 procedural visualizer and
music project. The archive's prebuilt `build/` outputs are intentionally
excluded: releases are reproducible from `src/` with the ACME assembler.

This is an independent assembly project. It is not an ArpSID plugin or C64SID
emulator component.
